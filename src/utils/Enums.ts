export enum Colors {
  Black,
  Blue,
  Gray,
  LightGray,
  Red,
  Transparent,
  White,
  WorkwolfColor,
}

export enum ButtonType {
  Button = 'button',
  Submit = 'submit',
}

export enum FontWeight {
  Thin,
  Light,
  Regular,
  Semibold,
  Bold,
}
